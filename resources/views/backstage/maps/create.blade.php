{{-- @extends('layouts.backstage')

@section('page-content')
<div class="container-fluid">

    <!-- Page Heading -->
    
    <!-- DataTales Example -->
    <div class="card shadow mb-4">
      <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">收藏地點 | 新增</h6>
      </div>

        <form action="{{ route('backstage.maps.store') }}" method="POST">
          @csrf
          <p class="text-primary">資訊 Info</p>
          <div class="mb-3">
            <label for="name" class="form-label">名稱 Name</label>
            <input type="text" class="form-control" id="name" name="name" />
          </div>
          <button class="btn btn-outline-primary btn-block">完成</button>
        </form>

    </div>
  </div>

@endsection --}}
